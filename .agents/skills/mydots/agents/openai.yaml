interface:
  display_name: "Mydots"
  short_description: "Repo-specific patterns and workflows for mydots"
  default_prompt: "Use the mydots repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true