# Vim swap and temp files
*.swp
*.swo
*~
.netrwhist

# OS generated files
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor & IDE
######################
.vscode
.history
*.code-workspace

# Packages
######################
# Prefer unpacked source; git handles compression natively
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# Logs and databases
######################
*.log
*.sql
*.sqlite

# Tool-local config (machine-specific, never commit)
######################
**/.claude/settings.local.json

# Cloned upstream repos for corpus research (CLAUDE.md - File Management)
######################
# Clones are read-only research material, re-cloneable from their remote.
# Matches a directory of this name at any depth. If a project legitimately
# tracks one, override in that repo's own .gitignore with: !repos/
repos/
ai-artifacts/repos/
scratch/
# Always track empty directories
!.gitkeep
.rtfm/
